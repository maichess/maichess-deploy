{{/*
maichess-bot-arena-service — orchestrates bot-vs-bot setups and tournaments.
Since Kafka task 18 it reacts to match completions by consuming match.events.v1
(MatchEnded, consumer group "bot-arena-completion") instead of polling
match-manager over gRPC. KAFKA_BOOTSTRAP is injected by the deployment macro when
kafka.enabled; the consumer group is a code constant, so no extra env is needed.
*/}}
{{ include "maichess.app.deployment" (dict
  "name" "bot-arena-service"
  "image" (printf "%s/maichess-bot-arena-service:%s" .Values.maichess.imageRegistry (.Values.services.botArenaService.tag | default "main"))
  "ports" (list
    (dict "containerPort" 8080 "name" "http")
  )
  "env" .Values.services.botArenaService.env
  "extraEnv" (list
    (dict "name" "ASPNETCORE_ENVIRONMENT" "value" .Values.global.environment)
    (dict "name" "Jwt__Key" "valueFrom" (dict "secretKeyRef" (dict "name" "maichess-app-secrets" "key" "jwt-secret")))
  )
  "probePort" 8080
  "resources" .Values.services.botArenaService.resources
  "affinity" (include "maichess.affinityBurstEligible" . | fromYaml)
  "tolerations" (list (include "maichess.tolerationBurst" . | fromYaml))
  "Release" .Release
  "Chart" .Chart
  "Values" .Values
) }}
---
{{ include "maichess.app.service" (dict
  "name" "bot-arena-service"
  "ports" (list
    (dict "port" 8080 "targetPort" 8080 "protocol" "TCP" "name" "http")
  )
) }}
